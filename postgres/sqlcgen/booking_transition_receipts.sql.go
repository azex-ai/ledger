// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: booking_transition_receipts.sql

package sqlcgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const getBookingTransitionReceiptByIdempotencyKey = `-- name: GetBookingTransitionReceiptByIdempotencyKey :one
SELECT id, booking_id, idempotency_key, to_status, channel_ref, amount, event_id, created_at
FROM booking_transition_receipts
WHERE idempotency_key = $1
`

func (q *Queries) GetBookingTransitionReceiptByIdempotencyKey(ctx context.Context, idempotencyKey string) (BookingTransitionReceipt, error) {
	row := q.db.QueryRow(ctx, getBookingTransitionReceiptByIdempotencyKey, idempotencyKey)
	var i BookingTransitionReceipt
	err := row.Scan(
		&i.ID,
		&i.BookingID,
		&i.IdempotencyKey,
		&i.ToStatus,
		&i.ChannelRef,
		&i.Amount,
		&i.EventID,
		&i.CreatedAt,
	)
	return i, err
}

const insertBookingTransitionReceipt = `-- name: InsertBookingTransitionReceipt :one
INSERT INTO booking_transition_receipts (booking_id, idempotency_key, to_status, channel_ref, amount, event_id)
VALUES ($1, $2, $3, $4, $5, $6)
ON CONFLICT (idempotency_key) DO NOTHING
RETURNING id, booking_id, idempotency_key, to_status, channel_ref, amount, event_id, created_at
`

type InsertBookingTransitionReceiptParams struct {
	BookingID      int64          `json:"booking_id"`
	IdempotencyKey string         `json:"idempotency_key"`
	ToStatus       string         `json:"to_status"`
	ChannelRef     string         `json:"channel_ref"`
	Amount         pgtype.Numeric `json:"amount"`
	EventID        int64          `json:"event_id"`
}

// Durable idempotency record for one Transition application when the caller
// opted in via TransitionInput.IdempotencyKey (I-3; see that field's doc
// comment for why it is opt-in). Mirrors InsertReservationOperationReceipt's
// pattern: on a replayed key this inserts nothing and returns no row, and
// the caller then fetches the existing receipt and compares payloads.
func (q *Queries) InsertBookingTransitionReceipt(ctx context.Context, arg InsertBookingTransitionReceiptParams) (BookingTransitionReceipt, error) {
	row := q.db.QueryRow(ctx, insertBookingTransitionReceipt,
		arg.BookingID,
		arg.IdempotencyKey,
		arg.ToStatus,
		arg.ChannelRef,
		arg.Amount,
		arg.EventID,
	)
	var i BookingTransitionReceipt
	err := row.Scan(
		&i.ID,
		&i.BookingID,
		&i.IdempotencyKey,
		&i.ToStatus,
		&i.ChannelRef,
		&i.Amount,
		&i.EventID,
		&i.CreatedAt,
	)
	return i, err
}
