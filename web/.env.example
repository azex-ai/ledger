# Client-exposed (inlined into the browser bundle at build):
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_API_KEY=
# Server-only (never sent to the browser; used by RSC prefetch):
LEDGER_API_URL_INTERNAL=
LEDGER_API_KEY=
