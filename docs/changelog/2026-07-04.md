# Changelog - 2026-07-04

## [00:30] Balance breakdown（available/pending/locked/total）+ Reserve 切 available 口径

**类型**：feat (breaking)
**概述**：classifications 新增 balance_role 语义标签；新增 `GetBalanceBreakdown` / `GET /balances/{holder}/{currency}/breakdown`（total = available + locked + pending）；Reserve 可用余额基数从「全部 classification 求和」改为「仅 role=available」。
**影响文件**：
- `postgres/sql/migrations/032_balance_role.{up,down}.sql`
- `core/types.go`, `core/interfaces.go`
- `postgres/ledger_store.go`, `postgres/reserver_store.go`, `postgres/classification_store.go`, `postgres/convert.go`
- `presets/templates.go`, `presets/pending.go`
- `server/handler_balances.go`, `server/routes.go`
- `docs/INVARIANTS.md`（I-11 改为 available 口径 + 6 个新 pinned test）, `docs/openapi.yaml`

**详情**：
- 旧 Reserve 口径的三个资金洞被堵死：未确认入金（pending）可被预留；提现锁定资金（lock_funds 后求和不变）可二次预留；fee_expense 等 debit-normal 费用科目虚增可预留额度
- preset 安装对存量 `''` role 幂等升级，非空冲突报 ErrConflict
- role 不进 dims cache（cache 只存不可变字段），聚合直查 config 表；pool 模式单 REPEATABLE READ 事务保证 role 求和与 holds 同快照

## [00:30] 移除 card bundle —— 渠道不进账本

**类型**：refactor (breaking)
**概述**：删除 `presets/card.go`（card_account / card_topup / card_topup_settle*）。卡、支付宝、微信、链上等充提渠道属于账本外部 rail（channel adapter / 消费方系统），卡余额镜像属对账层；消费方需要时可运行时注册自己的 classification。
**影响文件**：`presets/card.go`（删除）、`presets/templates.go`、`presets/fee.go`、`ledger.go`、`README.md`、`CLAUDE.md`

## [00:30] 修复 sqlc CI gate：migration 031 展开为平铺 DDL

**类型**：fix
**概述**：c1779a3 把 031 重写为 `DO $$ EXECUTE format()` 动态 DDL 后，sqlc 静态解析器看不到 uid 列，主干 `make sqlc` / `make sqlc-diff` 一直失败。展开为每表平铺 `ADD COLUMN → backfill → SET NOT NULL`（运行时语义不变，无外部用户期允许改 migration）。
**影响文件**：`postgres/sql/migrations/031_uid_identifiers.up.sql`

**Session**：`~/.claude/session-summaries/2026-07-04-00-30.md`

## [01:00] Review 修复批：4 Critical + 9 Major 全部收口

**类型**：fix (breaking)
**概述**：2026-07-03 金融账本深度 review 的全部剩余问题一次修复（M2 已提前单独修）。
**要点**：
- C1 ReverseJournal 事务化 + 行锁（并发双重全额反转堵死，029 删唯一索引后的裸奔期结束）
- C2 migration 033：journals 防篡改触发器补 effective_at/uid
- C3 后台任务 advisory lock 绑定专属连接（解锁失败 hijack-close），全 fleet 静默停摆隐患消除
- C4 migration 034：SettlePartial 幂等（settlement legs 表），重试不再双倍结算
- M1 pending confirm/cancel 锁下幂等复检；M3 migration 035 恢复 reservations.journal_id FK
- M4 reconcile report 去内部 id（uid/code 化 + 源码扫描 pin）；M9 Check#2 以 checkpoint 水位为界，假阳性消除
- M5 读端点纳入 API-key 鉴权（k8s 探针除外）；M6 精度校验覆盖 booking/settle 全路径
- M7 事件投递 claim-token + callback 不断链 + 本地投递改 at-least-once
- M8 Template Render 拒绝非正 HolderID；M10 list 契约统一 {list, next_cursor} + reservations keyset 分页
- Minor：金额拒科学计数法、ledgerd 超时配置、RunInTx 注释修正
**Commit**：d7b1452（全量 -race -count=1 / lint 0 issues / sqlc-diff 全绿）

**Session**：`~/.claude/session-summaries/2026-07-04-00-30.md`
