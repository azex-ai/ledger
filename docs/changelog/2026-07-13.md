# Changelog - 2026-07-13

## [11:34] 加密充值 + 归集 + M3 补偿控制 + 完整前端

**类型**：feat
**概述**：多链共享地址（CREATE2）加密充值 + 归集作为 ledger 库可选默认方案，含 M3 信任模型补偿控制与完整 @azex/ledger-react 前端；合并 feat/crypto-deposit → main。

**详情**：
- **入账/归集**：`chains/evm` 独立 go module（geth 隔离）watcher（getLogs 分片、reorg-safe TxLogSeq）+ scanner + sweeper（batchSweep + nonce 持久化 + gas-bump）+ 本地私钥 signer；service 编排 IngestDeposit（幂等归一化）+ watcher/sweep job；presets sweep lifecycle（无 journal）。
- **M3 补偿控制**：deposit lifecycle 加 `review` 非终态；金额阈值门 `AutoCreditCeiling` + 双 provider 对账 `DepositConfirmer` → 超阈值/不一致进人工 review；approve/reject 端点补过账；secure-by-default（未配 ceiling fail-fast，显式无上限用 `UnboundedAutoCredit` 哨兵）；新不变量 I-19/I-20/I-21。
- **前端**（shadcn + heroui 双 skin + headless core）：operator 审核队列（approve 确认摩擦 + reject 必填 reason + 安全乐观 UI）、sweep 监控、holder 充值地址卡（wallet 域、地址 + QR、无内部字段泄漏）。
- **holder 端点**：holder-token-scoped `/holder/deposit-address`（身份只信 token，修复前端两域隔离）。

**Review**：后端 2 轮 opus code+security 评审（C1 凭空铸币 / MJ1 secure-by-default 已修，见 `docs/bugs/2026-07-11-*review.md`），前端 house-rules 门禁通过。

**验证**：Go `-race` 全绿、`chains/evm` 绿、web 132 tests 绿、vet/lint/sqlc-diff 干净。

**Follow-up**：docs/frontend.md 补充、M3 mi2/mi5 加固、openapi CI 校验（见 bus board #21/#22/#23）。

**Session**：`~/.claude/session-summaries/2026-07-13-11-34.md`
