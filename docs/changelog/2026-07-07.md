# Changelog - 2026-07-07

## [22:56] ledger-react 0.3.0：三消费面（shadcn 皮肤 / HeroUI 皮肤 / headless）+ fullstack 示例

**类型**：feat
**概述**：`@azex/ledger-react` 拆出三个消费面——根入口 shadcn 风格皮肤（自包含 scoped preflight + tokens）、`./heroui` HeroUI v3 皮肤（optional peer）、`./headless`（仅 client/hooks/provider）；新增 `examples/fullstack` 全栈快速上手示例。
**影响文件**：
- `web/packages/ledger-react/package.json` — 0.3.0；`./headless` / `./heroui` exports + `heroui.css`；`@heroui/react` optional peer
- `web/packages/ledger-react/src/heroui/**`（新增）— HeroUI 皮肤全套页面（13 页 + LedgerAdmin/sidebar/pagination-bar），与 shadcn 皮肤共享 headless core，页面逻辑保持镜像
- `web/packages/ledger-react/src/headless.ts`（新增）— headless 入口
- `web/packages/ledger-react/src/styles/{preflight,heroui}.css`（新增）、`theme.css` / `index.css` / `scripts/build-css.ts` — scoped preflight、dark/system 主题变体、双 CSS 产物
- `web/packages/ledger-react/src/provider/{provider,context}.tsx` — `appearance: light|dark|system`；`LedgerAppearanceContext` + `LedgerPortalContainerContext`（floating layer portal 回 `.ledger-root`）
- `web/packages/ledger-react/src/hooks/use-{deposits,withdrawals,reservations}.ts` — cursor 分页 `useInfiniteQuery`（`{list, next_cursor}` 契约，与 useJournals 一致）
- `web/packages/ledger-react/src/components/pagination-bar.tsx`（新增）+ 各 pages — 分页 UI 接入
- `examples/fullstack/`（新增）— chi backend 完整 serve ledger HTTP API + Next.js `web/`（shadcn 皮肤）与 `web-heroui/`（HeroUI 皮肤）scaffolds
- `CLAUDE.md` / `README.md` / `web/packages/ledger-react/README.md` — 文档同步

**详情**：
- npm 0.2.0 与 v0.4 server wire contract 不兼容（`/system/balances` 返回 `{list}` 而旧包期望数组），0.3.0 为对齐版本；发版待 push `ledger-react-v0.3.0` tag 触发 `ledger-react-publish.yml`
- 双皮肤纪律：改任一皮肤的页面逻辑必须同步另一皮肤，共享逻辑下沉 headless hooks

**Session**：`~/.claude/session-summaries/2026-07-07-22-56.md`
