# Changelog - 2026-07-08

## [14:40] Holder-scoped wallet surface（P1-P4 全链路）

**类型**：feat
**概述**：用户钱包读端面上游化为库能力——holder token principal、翻译后的 holder 交易投影、可挂载 HolderHandler、`@azex/ledger-react/wallet` 双皮肤组件四件套。设计档 `docs/plans/2026-07-08-holder-scoped-wallet-surface.md`。
**影响文件**：
- `postgres/`：migration 038（display_label）、holder 投影查询 + store + 集成测试
- `core/`：HolderReader port + HolderTransaction/HolderHold/HolderBalance 读模型
- `server/`：lht_ HMAC token（mint/校验/中间件）、3 个只读 handler、`HolderHandler` 独立子路由、ledgerd HOLDER_TOKEN_SECRET 特性门
- `presets/`：journal type 默认用户标签（expand-safe 播种）
- `docs/openapi.yaml`：holder 端面 + holderToken security scheme
- `web/packages/ledger-react`：`/wallet` `/wallet/heroui` `/wallet/headless` 三入口、getToken 401 刷新重试 client、双皮肤 WalletPanel/Balances/BalanceCard/TransactionList、渲染面内部词汇护栏测试；0.4.0（未发版）
- `examples/fullstack`：`/wallet` demo（拓扑 A：in-process mint + WalletPanel）

**详情**：
- 交易投影 grain = (journal, holder, currency) 净额，仅聚合 role-bearing classification（实现期修订：role-less 费用账户会把手续费净额对消隐身）
- 净零 journal 不产出行但推进 cursor；FX 两币两行；冲正带 reversal_of_uid
- 另修 review 遗留 10 条 + metadata 页分页接线（2ed1cd0）

**Session**：holder wallet surface 开工会话（P1 c318954 / P2 da8dd30 / P3 6d66202 / P4 ff4e6be）
