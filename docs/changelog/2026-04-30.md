# Changelog - 2026-04-30

## [19:30] Open-source readiness pass: invariants doc, fuzz tests, FX preset, runbook, OpenAPI, helm chart, CLI

**类型**：feat / test / docs
**概述**：跨 Must / Should / Could 三档交付开源就绪所需内容 — 写下 13 条
不变量、把核心校验补到 99.5% 覆盖率（fuzz 抓到一个 lifecycle 校验真 bug
并修复）、加 FX preset、写运维 runbook 与 OpenAPI、补 Prometheus 测试、
出 ledger-cli 与 Helm chart。

**影响文件**：

Backend (Go):
- `core/types.go` — `Lifecycle.Validate` 修正：`Transitions[initial] = []` 不再被接受
- `core/{pending,system_account,observability,template_edges,lifecycle_stress,reserve_stress,journal_invariants}_test.go` — 新增
- `core/testdata/fuzz/FuzzLifecycleValidate/72dc44896ff458f1` — fuzz 找到的 corpus seed
- `postgres/invariants_test.go` — 4 个不变量集成测试 (reversal chain / idempotency race / partition / money conservation)
- `postgres/benchmarks_test.go` — 4 个基准
- `internal/postgrestest/postgrestest.go` — `SetupDB(testing.TB)`
- `presets/fx.go`, `presets/fx_test.go` — 跨币种 FX preset
- `presets/templates.go` — `InstallExtendedPresets` 注册 FXBundle
- `observability/prometheus_test.go` — Prometheus adapter 测试
- `cmd/ledger-cli/main.go` — 只读运维 CLI

Docs:
- `docs/INVARIANTS.md` — 13 条不变量 (I-1..I-13)，每条 Why / Enforced by / Pinned by
- `docs/RUNBOOK.md` — 9 节运维手册 + 紧急停机流程
- `docs/openapi.yaml` — OpenAPI 3.1，32 paths × 34 schemas

Deploy:
- `deploy/helm/ledger/` — Helm chart 骨架
- `examples/embed/main.go` — 最小化 library embed 示例

**详情**：

Must (5)：
- `INVARIANTS.md` 把散落各处的规则收拢到一份机读契约（I-1 per-currency balance、
  I-2 append-only、I-3 idempotency、I-4 reserve TOCTOU、I-5 checkpoint+delta、
  I-6 NUMERIC(30,18)、I-7 NOT NULL exceptions、I-8 lifecycle FSM、I-9 holder 符号、
  I-10 event-journal atomicity、I-11 reservation 不超额、I-12 money conservation、
  I-13 partition coverage）
- `TestReversalChainIntegrity` — 任何 journal 仅可被 reverse 一次，A+¬A 净额为零
- `TestIdempotency_ConcurrentSameKey` — 100 goroutine 同 key，恰好 1 笔成功
- `TestPartitionBoundary_DefaultCatchesOutsideRanges` — default partition 与跨分区读
- `TestMoneyConservation_Network` — 10 用户 × 200 次随机转账，3 个角度验证守恒
- FX preset：双 template，每币种独立平衡，settlement 吸收净持仓

Fuzz 实战：
`FuzzLifecycleValidate` 5 秒内发现 `Lifecycle.Validate` 接受 `Transitions[initial]=[]`
（map 有键但出边列表为空），直接修复并固化 corpus seed。

Should (4)：
- 运维 runbook：reconciliation/solvency/rollup/webhook 四类报警的处置
- 4 个基准：PostJournal single-account / fanout，GetBalance cold checkpoint，
  Reserve+Settle
- OpenAPI 3.1 spec
- 库导入最小示例

Could (3)：
- Prometheus adapter 测试（adapter 之前已实现，缺测试）
- ledger-cli：balance / journals / trace / reconcile --full / solvency / health
- Helm chart：Chart + values + deployment/service/ingress/secret + 非 root 安全上下文

测试覆盖：
- `core/` 78.2% → **99.5%**
- 全仓 `go test ./... -count=1` 全绿（postgres 集成与基准在无 Docker 时优雅跳过）

**Session**：`~/.claude/session-summaries/2026-04-30-19-30.md`
