# Changelog - 2026-09-07

## [00:50] Deposit-only credits 业务示例与 shadcn 消费集成

**类型**：feat/fix/ci/docs
**概述**：围绕 1 USDC 购买 1,000 credits，完善现有记账原语的组合、扣费边界和可替换钱包 UI，并修复外部 Go 项目的依赖解析失败。

**影响文件**：
- `examples/credits-topup/`、`docs/COOKBOOK.md`
- `web/packages/ledger-react/src/wallet/`、`web/packages/ledger-react/src/components/`
- `examples/fullstack/`、`web/src/lib/navigation.ts`
- `go.mod`、`go.work`、`internal/postgrestest/`、`scripts/test-consumer.sh`
- `.github/workflows/go-verify.yml`、`README.md`、`CLAUDE.md`

**详情**：
- 用事务组合 Reserve/Settle 与双币种 FX journals；示例覆盖固定、计量、流式扣费、零费用释放和完成事件重放，最终余额为 912.875 credits、零占用。
- 新增 8 个 PostgreSQL 测试，覆盖未确认入金、原子回滚、精度、过期、幂等冲突、超额拒绝与并发预算。
- shadcn 钱包保留空状态下的充值操作，向操作回调传递币种余额，支持区域 slots 和交易 renderItem，并暴露可触摸/键盘访问的完整金额。
- 默认 shadcn 示例移除提现导航与页面；充值地址仅在宿主配置真实 provider/network 后展示。HeroUI 源码未改动。
- 将无效零版本的 PostgreSQL 测试辅助模块收回根模块内部包；新增 workspace 外部消费测试，验证 tidy/build 及生产 import graph。
- Next.js 独立宿主使用本地 npm tarball 验证，补齐无需 Tailwind 的宿主布局 CSS。
- 首次推送的 CI 抓出包内类型检查依赖已有 `dist`：包内 TypeScript self-import 现解析源码，无构建产物检查通过；宿主仍使用实际包导出。
- 测试 fixture 纳入根扫描后暴露 `GO-2026-6354`/`GO-2026-6355`；所有 Go 模块的 `golang.org/x/crypto` 升至修复版本 `v0.56.0`，根模块同步其所需的 `x/text v0.41.0`。

**验证**：Go build/vet 与资金流回归、React 241 项测试、包与两套 Next.js 构建、桌面/移动浏览器检查通过。通用静态扫描的既有项和完整验证范围见[验收记录](../plans/2026-09-06-deposit-credits-polish.md)。真实链上配置、自动购入任务及业务策略缺口见[独立记录](../gaps/deposit-credits.md)。

**Session**：`~/.claude/session-summaries/2026-09-07-00-50.md`
