# Changelog - 2026-04-25

## [01:10] Ledger hardening: per-currency balance, deposit tolerance, claim-based queues

**Type**: feat/refactor
**Summary**: Major hardening pass — per-currency balance enforcement, deposit tolerance settlement, claim-based queue processing, idempotency improvements, error normalization, and comprehensive test coverage.

**Impact files**:
- `core/booking.go`, `core/journal.go`, `core/template.go`, `core/reserve.go`, `core/interfaces.go`
- `postgres/errors.go` (NEW), `postgres/booking_store.go`, `postgres/event_store.go`, `postgres/rollup_adapter.go`, `postgres/ledger_store.go`, `postgres/classification_store.go`, `postgres/webhook_subscriber_store.go` (NEW), `postgres/migrate.go`, `postgres/convert.go`
- `postgres/sql/migrations/004,005,012,017` (schema changes)
- `presets/templates.go` (NEW), `presets/tolerance.go` (NEW)
- `service/reconcile.go`, `service/snapshot.go`, `service/rollup.go`, `service/worker.go`, `service/delivery/webhook.go`
- `server/handler_journals.go`, `server/handler_metadata.go`, `server/routes.go`
- `CLAUDE.md`, `docs/api.md`

**Details**:
- Per-currency journal balance validation (Go + DB deferred constraint trigger)
- Deposit tolerance settlement: plan/execute pattern for shortfall/overage handling
- Template preset installation system with conflict detection
- Claim/lease queue processing with SKIP LOCKED for rollup and event delivery
- Idempotent transition handling for duplicate webhook callbacks
- Double-reversal protection via partial unique index
- PG error code → domain error normalization layer
- Input validation on CreateBookingInput, TransitionInput, TemplateInput
- Active → Settled direct reservation transition
- Per-currency global reconciliation, missing-checkpoint account reconciliation
- Entry-based historical balance snapshots (replaces checkpoint-based)
- Webhook subscriber store, webhook deliverer nil-safety
- 10 new test files with comprehensive coverage

**Session**: `~/.claude/session-summaries/2026-04-25-01-10.md`
